# Changelog

All notable changes to this project will be documented in this file.


## [0.1.0] - 2025-09-01
### Added
- Initial release of **otp-kit** 🎉
- Support for Random OTP generation:
  - `numeric` → digits only (0-9)
  - `alphabetic` → letters only (a-zA-Z)
  - `alphanumeric` → digits + letters
  - `custom` → custom-defined characters
- OTP verification with optional expiry handling
- TypeScript support with strict types
- Unit tests for OTP generation and verification
